<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/blue_grey_100" /> <!-- Color del contorno: gris (#808080) -->
    <corners android:radius="@dimen/_10sdp" /> <!-- Radio de esquinas redondeadas: 50dp -->
</shape>
